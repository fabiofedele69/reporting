Hi Mayur,

I am investigating the behavior of semantic model permissions in the UAT Power BI workspace.

Current situation:

* In DEV environment, the “Manage permissions” option is available on semantic models and allows Build permissions to be assigned to users.
* In UAT environment, the same option is currently not visible/available on the semantic models, even though I published the reports and semantic models myself and we both have workspace roles assigned through the BBS/MIM process.

Since you originally requested/provisioned the workspace, I would like to validate whether there is any ownership or governance difference attached to the original requester of the workspace.

Could you please perform the following test?

Test:

1. Create a very small test report (.pbix)
2. Publish it into the same UAT workspace
3. Open the corresponding semantic model in Power BI Service
4. Verify whether the “Manage permissions” option is available for you

Expected outcomes:

* If you can see/manage permissions:
  this may indicate that the original workspace requester has additional ownership/governance privileges.

* If you also cannot see “Manage permissions”:
  then the issue is likely related to UAT governance restrictions or tenant-level Power BI/Fabric policies rather than user-specific permissions.

In case the test fails as well, we may need to:

* involve the Power BI/Fabric admin team
* verify tenant/workspace governance settings
* confirm whether Build permissions are intentionally managed only through BBS/MIM groups in UAT
* evaluate whether App-based permission inheritance should be used instead of manual semantic model sharing

Please let me know the outcome once you have tested it.

Thanks,
Fabio
